<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Solution · Phoenix Framework</title>
  <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
  <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>

<body>
  <header>
    <section class="container">
      <nav role="navigation">
      <%= if current_user(@conn) do%>
        <%= if current_role(@conn) == "admin" do%>
          <ul>
          <li><%= link "Home", to: Routes.page_path(@conn, :index)  %></li>
            <li><%= link "Register User", to: Routes.user_path(@conn, :new)  %></li>
            <li><%= link "Users Overview", to: Routes.user_path(@conn, :overview)  %></li>
            <li><%= link "Register Product", to: Routes.product_path(@conn, :new)  %></li>
            <li><%= link "Products Overview", to: Routes.product_path(@conn, :overview)  %></li>
            <%= cart_item_count(@conn) %> item(s) in your cart
            <li><%= link "Show cart", to: Routes.cart_path(@conn, :show)  %></li>
            <li><%= link "Show Orders", to: Routes.order_path(@conn, :show)  %></li>
          </ul>
        <% else %>
          <ul>
            <li><%= link "Home", to: Routes.page_path(@conn, :index)  %></li>
            <li><%= link "Users Overview", to: Routes.user_path(@conn, :overview)  %></li>
            <li><%= link "Products Overview", to: Routes.product_path(@conn, :overview)  %></li>
            <%= cart_item_count(@conn) %> item(s) in your cart
            <li><%= link "Show cart", to: Routes.cart_path(@conn, :show)  %></li>
            <li><%= link "Show Orders", to: Routes.order_path(@conn, :show)  %></li>
          </ul>
        <% end %>
      <% else %>
        <ul>
          <li><%= link "Home", to: Routes.page_path(@conn, :index)  %></li>
          <li><%= link "Register User", to: Routes.user_path(@conn, :new)  %></li>
          <li><%= link "Products Overview", to: Routes.product_path(@conn, :overview)  %></li>
        </ul>
      <% end %>
      </nav>
      <ul>
        <li><%= new_locale @conn, :en, "English" %></li>
        <li><%= new_locale @conn, :nl, "Nederlands" %></li>
      </ul>
    </section>
  </header>
  <main role="main" class="container">
    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    <%= @inner_content %>
  </main>
</body>

</html>
